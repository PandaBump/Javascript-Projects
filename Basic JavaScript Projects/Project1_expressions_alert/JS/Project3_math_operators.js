var z = function.mathRandom();
var x = window.prompt("What is the first number?");
var y = window.prompt("What is the second number?");
alert("Calculating...");
function calculatingAdd(x, y) {
    return var 1 (x + x);
  }
  function calculatingSub(x, y) {
    return x - x;
  }
  function calculatingMulti(x, y) {
    return x * x;
  }
  function calculatingRemain(x, y) {
    return x % x;
  }
  function calculatingIncre(x, y) {
    return x ++ x;
  }
  function calculatingDecre(x, y) {
    return x -- x;
  }
function showResults{
    document.write("Your numbers added are: " + calculatingAdd);
    document.write("Your numbers subtracted are: " + calculatingSub);
    document.write("Your numbers multiplied are: " + calculatingMulti);
    document.write("The remainder of your numbers are: " + calculatingRemain);
    document.write("The increments of your numbers are: " + calculatingIncre);
    document.write("The decrements of your numbers are: " + calculatingDecre);
    document.write("Next time, try using the number " + z + " in your choices.");
}
